# FasMail Panel - Environment Variables
# Copy this file to .env and modify as needed

# Database password (used by both PostgreSQL container and the app)
DB_PASSWORD=fasmail_secret

# JWT secret (leave empty to auto-generate on first run)
JWT_SECRET=
